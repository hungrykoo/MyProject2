<?xml version="1.0" encoding="UTF-8"?>
<!-- 
	1) XML (필요한 데이터를 문서로 저장) : Document, HTML(View)
		-> 문서형 데이터 베이스
		-> 운영체제의 호환성이좋다.
		-> 모든프로그램 언어에서 같은 내용을 출력 할 수 있다.
		-> XML
			# XML 구MXLML(
			# 문서 제작하는 방법 (DTD, XSD)
			# 변환(XSLT) : html, pdf
			# 언어(자바)에서 활용 : DOM, SAX(******): XML Parser
							====,=====
					메모리에 저장한 형태, 한줄씩 읽어서 데이터를 가지고 오다.
		
		1. 구분이 되어 있다.(반정형화 데이터)
			XML : 필요한 데이터 저장시에 <a>()</a) <a href="()">
				태그 사이, 속성
			ML : 태그와 속성으로 만들어진다.
				태그 : 여는 태그<a>, 닫는태그 </a>,
					Empty 태그 (딴독태그) <input />
				
		2. XML : 태그는 지정된 것이 없다. (사용자 정의) => 시맨틱으로 제작
			1) XML 문법
				= 태그를 만들 경우
				#알파벳, 한글을 사용한다. ( 알파벳 대소문자 구별함)
				 HTML : <A>=<a>, XML : <A>!=<a>
				 JAVA : int A; int a;
				#숫자를 사용 할 수 있다. : 단 앞에 사용금지
					<2aaa>(X) => <a2> <a3> (o)
				#특수문자 사용이 가능: (_), (-), . , $ , : 
					<c:forEach),<file name>,<file.size>
				#키보드(예약어) 는 사용할 수 없다 => xml
				 = 계층구조 : 전체 루트태그
				 =열고 닫는 태그가 반드시 일치 (순서)
				 <a><b><c></c></b></a>
				 #속성 값 지정할떄는 반드시 "", ''
				 <table width=300>(x)
				 <table width="300"<O>
				 
				 
				 
-->
<sawon> <!-- table명 -->
<list> <!-- ROW (record)  -->
<sabun>1</sabun> <!-- Column  -->
<name>홍길동</name>
<dept>개발부</dept>
<sex>남자</sex>
<age>26</age>
</list>
<list>
<sabun>2</sabun>
<name>심청이</name>
<dept>개발부</dept>
<sex>남자</sex>
<age>26</age>
</list>
<list>
<sabun>3</sabun>
<name>박문수</name>
<dept>개발부</dept>
<sex>남자</sex>
<age>28</age>
</list>

</sawon>
